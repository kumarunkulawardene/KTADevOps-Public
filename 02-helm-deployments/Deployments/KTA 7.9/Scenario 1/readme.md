Scenario  
- All in One KTA Web\App
- Runtime configuration passed to new containers from AKS resources

Note:
- Follow the relevant KTA documentation for runtime configuration requirements.
- 
To Do
- License Service
    -   Ensure the gateway timeout is large enough to allow the UI prompt to complete (>30secs).
 
    Investigate how to pass the custom parameter within AKS to trigger the config file encryption.

