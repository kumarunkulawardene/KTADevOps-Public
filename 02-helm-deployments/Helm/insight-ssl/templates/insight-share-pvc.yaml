apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: insight-azurefile-pvc
  namespace: {{ .Values.deployment.environmentPrefix }}kta
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 5Gi