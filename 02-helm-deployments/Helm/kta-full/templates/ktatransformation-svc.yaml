apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.deployment.environmentPrefix }}kta-transformation-service  
  namespace: {{ .Values.deployment.environmentPrefix }}kta  
spec:
  type: ClusterIP
  ports:
  - port: 9001
  selector:
    app: {{ .Values.deployment.environmentPrefix }}kta-transformation