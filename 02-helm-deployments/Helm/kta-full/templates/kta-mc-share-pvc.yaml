apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kta-mc-azurefile-pvc
  namespace: {{ .Values.deployment.environmentPrefix }}kta
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 5Gi