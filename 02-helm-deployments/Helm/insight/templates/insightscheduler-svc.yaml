apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.deployment.environmentPrefix }}insight-scheduler-service
  namespace: {{ .Values.deployment.environmentPrefix }}kta 
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.deployment.environmentPrefix }}insight-scheduler
  ports:
    - port: 13640